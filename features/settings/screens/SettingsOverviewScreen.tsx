import { useState } from 'react';
import { Text, View } from 'react-native';
import { Avatar, BottomBar, Dialog, Icon, ListItem, RootScreen, StatusBadge, TopBar } from '../../../shared/components/ui';
import { radii, spacing, typography } from '../../../theme/scales';
import { useAppTheme } from '../../../theme/useAppTheme';
import { useAuthStore } from '../../../stores/useAuthStore';
import { tabs, type MainTabId } from '../../home/mainTabs';
import { getInitials } from '../../home/homeUtils';
import { useUserImageUrl } from '../hooks/useUserImageUrl';

interface Props { activeTab: MainTabId; onSelectTab: (tab: MainTabId) => void; onBack: () => void; onOpen: (target: 'profile' | 'appearance' | 'notifications' | 'security' | 'privacy' | 'subscription') => void }
export function SettingsOverviewScreen({ activeTab, onSelectTab, onBack, onOpen }: Props) { const { colors } = useAppTheme(); const user = useAuthStore((s) => s.user); const signOut = useAuthStore((s) => s.signOutUser); const [logoutOpen, setLogoutOpen] = useState(false); const imageUrl = useUserImageUrl(user?.filename, user?.id); const premium = String(user?.subscription).toLowerCase() === 'premium'; const leading = (name: 'profile' | 'visibilityOn' | 'notifications' | 'lock' | 'info' | 'file') => <View style={{ width: 40, height: 40, alignItems: 'center', justifyContent: 'center', borderRadius: radii.full, backgroundColor: colors.surfaceVariant }}><Icon name={name} color={colors.primary} /></View>; return <><RootScreen header={<TopBar title="Vasco" context="detail" onBack={onBack} />} bottomBar={<BottomBar items={tabs} activeId={activeTab} onSelect={onSelectTab} testID="main-tabs" />} contentContainerStyle={{ gap: spacing.lg, paddingBottom: spacing.xxl }} testID="settings-overview"><View style={{ gap: spacing.xs }}><Text accessibilityRole="header" style={{ color: colors.textPrimary, fontFamily: typography.fonts.bold, fontSize: typography.sizes.xxl }}>Compte et réglages</Text><Text style={{ color: colors.textSecondary, fontFamily: typography.fonts.regular, fontSize: typography.sizes.sm }}>Gérez Vasco selon vos préférences</Text></View><View style={{ alignItems: 'center', gap: spacing.sm }}><Avatar initials={getInitials(user?.prenom)} imageUrl={imageUrl} accessibilityLabel="Photo de profil" size={76} /><Text style={{ color: colors.textPrimary, fontFamily: typography.fonts.semiBold, fontSize: typography.sizes.xl }}>{user?.prenom || 'Compte Vasco'}</Text><Text style={{ color: colors.textSecondary, fontFamily: typography.fonts.regular, fontSize: typography.sizes.sm }}>{user?.email}</Text><StatusBadge label={premium ? 'Vasco Pro · Actif' : 'Vasco Gratuit'} tone={premium ? 'success' : 'neutral'} /></View><View><ListItem title="Profil" subtitle="Nom, photo et adresse e-mail" leading={leading('profile')} onPress={() => onOpen('profile')} /><ListItem title="Apparence" subtitle="Système, clair ou sombre" leading={leading('visibilityOn')} onPress={() => onOpen('appearance')} /><ListItem title="Notifications" subtitle="Rappels et activité des groupes" leading={leading('notifications')} onPress={() => onOpen('notifications')} /><ListItem title="Sécurité" subtitle="Mot de passe et connexion" leading={leading('lock')} onPress={() => onOpen('security')} /><ListItem title="Données et confidentialité" subtitle="Suppression du compte" leading={leading('file')} onPress={() => onOpen('privacy')} /><ListItem title="Abonnement et support" subtitle={premium ? 'Formule Pro, aide et informations' : 'Formule gratuite, aide et informations'} leading={leading('info')} onPress={() => onOpen('subscription')} /><ListItem title="Se déconnecter" leading={leading('lock')} onPress={() => setLogoutOpen(true)} /></View></RootScreen><Dialog open={logoutOpen} title="Se déconnecter ?" description="Vous devrez vous identifier à nouveau pour accéder à Vasco." confirmLabel="Se déconnecter" onClose={() => setLogoutOpen(false)} onConfirm={() => signOut()} /></>; }
