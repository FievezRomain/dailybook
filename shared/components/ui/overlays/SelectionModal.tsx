import { useMemo, useState, type ReactNode } from 'react';
import { FlatList, Modal, Pressable, Text, TextInput, View } from 'react-native';
import { componentTokens } from '../../../../theme/componentTokens';
import { alpha } from '../../../../theme/primitives';
import { radii, spacing, typography } from '../../../../theme/scales';
import { useAppTheme } from '../../../../theme/useAppTheme';
import { Button } from '../actions';
import { Icon } from '../icons';
import { resolveOverlaySurfaceColor } from './overlayStyles';
import { filterSelectionOptions, toggleSelection } from './selectionUtils';

export interface SelectionOption<TId extends string = string> { id: TId; label: string; visual?: ReactNode; disabled?: boolean }
export interface SelectionModalProps<TId extends string = string> { open: boolean; title: string; options: readonly SelectionOption<TId>[]; selectedIds: readonly TId[]; mode?: 'single' | 'multi'; onChange: (ids: TId[]) => void; onConfirm: () => void; onClose: () => void; confirmLabel?: string; testID?: string }

export function SelectionModal<TId extends string>({ open, title, options, selectedIds, mode = 'single', onChange, onConfirm, onClose, confirmLabel = 'Confirmer', testID }: SelectionModalProps<TId>) {
  const { colors } = useAppTheme(); const [query, setQuery] = useState(''); const filtered = useMemo(() => filterSelectionOptions(options, query), [options, query]);
  return <Modal visible={open} transparent animationType="fade" statusBarTranslucent onRequestClose={onClose}><Pressable onPress={onClose} style={{ flex: 1, alignItems: 'center', justifyContent: 'center', padding: spacing.lg, backgroundColor: colors.overlay }}><Pressable onPress={(event) => event.stopPropagation()} accessibilityRole="menu" accessibilityLabel={title} testID={testID} style={{ width: '100%', maxWidth: componentTokens.picker.selection.width, height: componentTokens.picker.selection.height, gap: 12, paddingHorizontal: spacing.md, paddingVertical: 20, borderRadius: radii.modal, borderWidth: 1, borderColor: colors.border, backgroundColor: resolveOverlaySurfaceColor(colors), shadowColor: alpha.black16, shadowOffset: { width: 0, height: 8 }, shadowOpacity: 1, shadowRadius: 8, elevation: 8 }}><Text style={{ color: colors.textPrimary, fontFamily: typography.fonts.semiBold, fontSize: typography.sizes.lg, lineHeight: 24 }}>{title}</Text><View style={{ height: 48, flexDirection: 'row', alignItems: 'center', gap: spacing.sm, paddingHorizontal: 14, borderRadius: radii.pill, backgroundColor: colors.surfaceVariant }}><Icon name="search" size="sm" color={colors.textSecondary} /><TextInput value={query} onChangeText={setQuery} placeholder="Rechercher…" placeholderTextColor={colors.textSecondary} accessibilityLabel="Rechercher" style={{ flex: 1, color: colors.textPrimary, fontFamily: typography.fonts.regular, fontSize: typography.sizes.control }} />{query ? <Pressable accessibilityRole="button" accessibilityLabel="Effacer la recherche" onPress={() => setQuery('')}><Icon name="close" size="sm" /></Pressable> : null}</View><FlatList data={filtered} keyExtractor={(item) => item.id} contentContainerStyle={{ gap: spacing.xs }} style={{ flex: 1 }} keyboardShouldPersistTaps="handled" renderItem={({ item }) => { const selected = selectedIds.includes(item.id); return <Pressable accessibilityRole={mode === 'single' ? 'radio' : 'checkbox'} accessibilityLabel={item.label} accessibilityState={{ selected, checked: selected, disabled: item.disabled }} disabled={item.disabled} onPress={() => onChange(toggleSelection(selectedIds, item.id, mode))} style={({ pressed }) => ({ height: componentTokens.picker.selection.optionHeight, flexDirection: 'row', alignItems: 'center', gap: 10, paddingHorizontal: spacing.sm, borderRadius: radii.md, backgroundColor: selected || pressed ? colors.surfaceVariant : colors.transparent, opacity: item.disabled ? 0.5 : 1 })}>{item.visual}<Text numberOfLines={1} style={{ flex: 1, color: colors.textPrimary, fontFamily: typography.fonts.semiBold, fontSize: typography.sizes.md }}>{item.label}</Text><View style={{ width: 20, height: 20, borderRadius: mode === 'single' ? radii.full : radii.xs, borderWidth: 2, borderColor: selected ? colors.primary : colors.border, backgroundColor: selected ? colors.primary : colors.transparent, alignItems: 'center', justifyContent: 'center' }}>{selected ? <Icon name="success" size="sm" color={colors.textOnPrimary} /> : null}</View></Pressable>; }} ListEmptyComponent={<Text style={{ color: colors.textSecondary, fontFamily: typography.fonts.regular, fontSize: typography.sizes.control, textAlign: 'center', padding: spacing.lg }}>Aucun résultat</Text>} /><Button label={confirmLabel} fullWidth disabled={selectedIds.length === 0} onPress={onConfirm} /></Pressable></Pressable></Modal>;
}
